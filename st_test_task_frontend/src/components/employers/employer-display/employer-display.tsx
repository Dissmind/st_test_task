import React from "react"
import {Employer} from "../../../types/employer";

interface EmployerDisplayProp {
    employer: Employer
}

export const EmployerDisplay = ({employer}: EmployerDisplayProp) => {

    return (
        <div>
            
        </div>
    )
}